- name: variables
  hosts: local
  connection: localhost

  tasks:
  - name: print variables
    ansible.builtin.debug:
      msg: "project is {{ PROJECT }}, environment is {{ ENVIRONMENT }}, component is {{ COMPONENT }}"
